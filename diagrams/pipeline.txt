# Diagrama da pipeline

```text
[Base dos Dados / BigQuery] --batch--> [Bronze] --+
[MySQL dims] ----------------batch--> [Bronze] --+--> [Silver] --> qualidade --> [Gold]
[Eventos JSON] --Kafka/file-sink----> [Bronze] --+                    |
                                                                  municipio / UF / Brasil
                                                                  evolucao temporal
                                                                        |
                                                              reports/ + BI / IA
```

Versão Mermaid (também em `docs/ARQUITETURA.md`):

```mermaid
flowchart LR
  BD[Base_dos_Dados] --> Batch
  MY[MySQL] --> Batch
  EV[Eventos] --> Stream
  Batch --> Bronze
  Stream --> Bronze
  Bronze --> Silver --> Gold --> Reports[reports_executivo]
```

Para arte final: importar este fluxo no Draw.io / Lucidchart.
